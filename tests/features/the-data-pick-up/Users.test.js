import React from 'react';
import { shallow } from 'enzyme';
import { expect } from 'chai';
// import { Users } from 'src/features/the-data-pick-up/Users';

describe('the-data-pick-up/Users', () => {
  it('renders node with correct class name', () => {
    const props = {
      theDataPickUp: {},
      actions: {},
    };
    // const renderedComponent = shallow(
    //   <Users {...props} />
    // );

    // expect(
    //   renderedComponent.find('.the-data-pick-up-users').getElement()
    // ).to.exist;
  });
});
